# 1. Change into your project directory
cd PythonAssignment

# 2. Initialize a new Git repo (if you haven’t already)
git init

# 3. (Optional) Cache your macOS credentials so you don’t have to re-enter them
git config --global credential.helper osxkeychain

# 4. Add the GitHub repository as “origin”
git remote add origin https://github.com/kasaralavamsi/PythonAssignment.git

# 5. Create and switch to a new ‘develop’ branch
git checkout -b develop

# 6. Stage all your files
git add .

# 7. Commit with a meaningful message
git commit -m "Initial commit – added Python assignment files"

# 8. Push ‘develop’ to GitHub and set the upstream
git push -u origin develop

# 9. (Optional) Verify
git status

